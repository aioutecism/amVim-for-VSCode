export class Motion {
	private _characterOffset: number = 0;
	private _lineOffset: number = 0;

	get characterOffset() { return this._characterOffset; }
	get lineOffset() { return this._lineOffset; }
}